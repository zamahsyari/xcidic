// import { fromJS } from 'immutable';
// import { makeSelectAddRecordDomain } from '../selectors';

// const selector = makeSelectAddRecordDomain();

describe('makeSelectAddRecordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
