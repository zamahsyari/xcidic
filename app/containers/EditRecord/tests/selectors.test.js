// import { fromJS } from 'immutable';
// import { makeSelectEditRecordDomain } from '../selectors';

// const selector = makeSelectEditRecordDomain();

describe('makeSelectEditRecordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
