// import { fromJS } from 'immutable';
// import { makeSelectDetailRecordDomain } from '../selectors';

// const selector = makeSelectDetailRecordDomain();

describe('makeSelectDetailRecordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
