// import { fromJS } from 'immutable';
// import { makeSelectRegisterDomain } from '../selectors';

// const selector = makeSelectRegisterDomain();

describe('makeSelectRegisterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
